#include <bits/stdc++.h>

using namespace std;

int main()
{
	int casos;
	scanf("%d", &casos);
	while(casos--){
		int n, l;
		scanf("%d %d", &n, &l);

		int base1, base2, h;
		scanf("%d %d %d", &base1, &base2, &h);

		double literPerPerson = l / n;

		
	}
}